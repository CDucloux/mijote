<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Fond de fenetre = couleur de fond du theme (jour/nuit, cf. splash_background),
             peint AVANT le premier rendu du WebView, y compris sous la barre systeme en
             edge-to-edge. Evite le flash gris de la zone systeme au premier affichage
             (fenetre par defaut du theme DayNight) avant que le WebView ne peigne son fond. -->
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>


    <!-- Splash natif reduit a un fond plat, sans logo : l'OS peint uniquement
         @color/splash_background (icone transparente), puis le splash web anime
         (#boot-splash, cf. index.html) prend le relais avec le logo + l'onde.
         Objectif : supprimer l'effet de double chargement (logo natif statique
         PUIS logo web anime) sur mobile, ne garder que le splash anime. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_none</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="android:background">@color/splash_background</item>
        <!-- Barres systeme transparentes des le lancement (edge-to-edge) : le fond du
             splash peint sous elles, plus de bande grise en haut avant le WebView. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>